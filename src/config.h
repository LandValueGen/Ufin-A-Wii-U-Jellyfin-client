#pragma once

// ---- Edit these before building ----
// Your Jellyfin server's LAN IP or hostname. No "http://", no port here.
#define UFIN_SERVER_HOST "xxx.xxx.xxx.xxx"
#define UFIN_SERVER_PORT 8096

// A real Jellyfin user account (create one for the Wii U if you want
// separate watch-history/permissions instead of reusing your own).
#define UFIN_USERNAME "name"
#define UFIN_PASSWORD "pass"
